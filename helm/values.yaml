## Default values for notification-service.
## This is a YAML-formatted file.
##
##     * * *   NOTE: SOME VALUES INCLUDE THE PLACEHOLDER "${RELEASE_NAME}",   * * *
##     * * *         WHICH MUST BE REPLACED BY THE ACTUAL RELEASE NAME        * * *
##
## Make sure you're in the correct k8s context, then:
##  $  helm upgrade --install ns -n notifications -f values-dev-cluster-notifications.yaml \
##         oci://ghcr.io/dataone/charts/notification-service --version [version] \
##             --debug --render-subchart-notes
##

## @section Application-Specific Properties for notification-service
##
ns:
  ## @param ns.logLevel The log level for the notification service.
  ## (trace, debug, info, warn, error, fatal)
  logLevel: warn

  ## @param ns.auth.api.baseUrl The base URL for the authentication API (typically the CN)
  ## @param ns.auth.api.authenticate The endpoint for authentication (CN uses '/diag/subject')
  ## @param ns.auth.api.userAgent The User-Agent string to use when making requests to the auth API
  ##
  auth:
    api:
      baseUrl: https://cn.dataone.org/cn/v2
      authenticate: /diag/subject
      userAgent: "DataONE notification-service/1.0"

  database:
    ## @param ns.database.dbName The name of the database used by the notification service.
    ##
    dbName: notificationdb

    ## @param ns.database.existingSecret (REQUIRED) Basic Auth Secret name for DB username & password
    ##
    existingSecret: "nsdb-cnpg-app"

    ## @param ns.database.jdbcHost (REQUIRED) database connection hostname (postgresql k8s service)
    ## Typically <cnpg-release-name>-cnpg-rw
    ##
    jdbcHost: "nsdb-cnpg-rw"

    ## @param ns.database.jdbcPort The port for the database connection.
    ## This is used by the application to connect to the database.
    ##
    jdbcPort: 5432

replicaCount: 1

image:
  repository: ghcr.io/dataoneorg/notification-service
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""


## This section builds out the service account more information can be found here:
##   https://kubernetes.io/docs/concepts/security/service-accounts/ServiceAccount
## @param serviceAccount.create Should a service account be created to run notifications?
## @param serviceAccount.annotations Annotations to add to the service account
## @param serviceAccount.name The name to use for the service account.
##        If not set, and 'create' is 'true', a name is generated using the 'fullname' template
##
serviceAccount:
  create: false
  annotations: {}
  name: ""

# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/ 
podAnnotations: {}
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

## This block is for setting up the ingress for more information can be found here:
## https://kubernetes.io/docs/concepts/services-networking/ingress/
## Ingress is a collection of rules that allow inbound connections to reach the endpoints defined
## by a backend. An Ingress can be configured to give services externally-reachable urls, load
## balance traffic, terminate SSL, offer name based virtual hosting etc.
##
ingress:
  ## @param ingress.enabled Enable or disable the ingress
  ##
  enabled: true

  ## @param ingress.className ClassName of the ingress provider in your cluster
  ##  Inspect available classes in your cluster using:    $ kubectl get ingressclasses
  ##
  ## className: "traefik" -- For Rancher Desktop (provided you have traefik enabled:
  ##     'preferences' -> 'kubernetes' -> 'enable traefik')
  ## className: "nginx" -- For production, or to use certificates locally. Also:
  ## - disable traefik ('preferences' -> 'kubernetes' -> uncheck 'enable traefik')
  ## - install nginx:
  ##   $  helm upgrade --install ingress-nginx ingress-nginx  \
  ##        --repo https://kubernetes.github.io/ingress-nginx \
  ##        --namespace ingress-nginx --create-namespace      \
  ##        --set controller.allowSnippetAnnotations=true
  ##
  className: "nginx"

  ## @param ingress.defaultBackend.enabled enable the optional defaultBackend
  ## ingress.defaultBackend (optional) default for any requests not matching pre-defined paths
  ## @param ingress.defaultBackend.enabled enable the optional defaultBackend
  ## If none of the hosts or paths match the HTTP request in the Ingress objects, the traffic is
  ## routed to your default backend. Example:
  ##
  ##   defaultBackend:
  ##     enabled: true
  ##     service:
  ##       name: myrelease-wordpress
  ##       port:
  ##         number: 80
  ##
  defaultBackend:
    enabled: false

  ## @param ingress.annotations [object] Annotations to add to the ingress;
  ## e.g. to use cert-manager to automatically issue TLS certificates for  the ingress hosts, you
  ## can add the following:
  ##
  ## ingress:
  ##   annotations:
  ##     cert-manager.io/cluster-issuer: "letsencrypt-prod"
  ##
  annotations:
    nginx.ingress.kubernetes.io/configuration-snippet: |
      # MetacatUI needs this Cors Setup

      set $cors 'true';

      if ($request_method = 'OPTIONS') {
        set $cors ${cors}options;
      }

      if ($cors = "true") {
        more_set_headers 'Access-Control-Allow-Origin: $http_origin';
        more_set_headers 'Access-Control-Allow-Credentials: true';
        more_set_headers 'Access-Control-Allow-Methods: POST, PUT, GET, DELETE, OPTIONS';
        more_set_headers 'Access-Control-Allow-Headers: DNT,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization';

        more_set_headers 'Access-Control-Max-Age: 1728000';
      }

      if ($cors = "trueoptions") {
        more_set_headers 'Access-Control-Allow-Origin: $http_origin';
        more_set_headers 'Access-Control-Allow-Credentials: true';
        more_set_headers 'Access-Control-Allow-Methods: POST, PUT, GET, DELETE, OPTIONS';
        more_set_headers 'Access-Control-Allow-Headers: DNT,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization';

        more_set_headers 'Access-Control-Max-Age: 1728000';
        more_set_headers 'Content-Type: text/plain charset=UTF-8';
        more_set_headers 'Content-Length: 0';
        return 204;
      }

  ## @param ingress.hosts [Object] List of hosts to be used for the ingress
  ## Each host must have a path defined, and the pathType must be set to either
  ## 'ImplementationSpecific', 'Exact', or 'Prefix'; e.g:
  ##   hosts:
  ##     - host: &hostname notifications.test.dataone.org
  ##       paths:
  ##         - path: /
  ##           pathType: Prefix
  hosts:
    - host: localhost
      paths:
        - path: /
          pathType: Prefix

  ## @param ingress.tls List of TLS configurations for the ingress
  ## Each TLS configuration must have a secretName defined, and the hosts must match the
  ## hosts defined in the ingress.hosts section. For example:
  ##   tls:
  ##     - hosts:
  ##         - *hostname
  ##       secretName: ingress-nginx-tls-cert
  tls: []

## Startup Probe
## Liveness and readiness probes are disabled until the startupProbe passes.
## If the startupProbe fails more than 'failureThreshold' times, the container is restarted.
##
## @param startupProbe.enabled Enable readinessProbe for notifications container
## @param startupProbe.enabled Enable startupProbe for the notifications container
## @param startupProbe.httpGet.path The url path to probe during startup
## @param startupProbe.httpGet.port The containerPort to probe
## @param startupProbe.successThreshold Min consecutive successes for probe to be successful
## @param startupProbe.failureThreshold No. of consecutive failures before the container restarted
## @param startupProbe.periodSeconds Interval (in seconds) between startup checks
## @param startupProbe.timeoutSeconds Timeout (in seconds) for each startup check
##
startupProbe:
  enabled: true
  httpGet:
    path: /
    port: 8080
  successThreshold: 1
  failureThreshold: 15
  periodSeconds: 10
  timeoutSeconds: 5

## Liveness Probe
## Periodic probe of container service liveness. If the probe fails, container will be restarted
##
## @param livenessProbe.enabled Enable livenessProbe for notifications container
## @param livenessProbe.httpGet.path The url path to probe.
## @param livenessProbe.httpGet.port The named containerPort to probe
## @param livenessProbe.periodSeconds Period seconds for readinessProbe
## @param livenessProbe.timeoutSeconds Timeout seconds for readinessProbe
## @param livenessProbe.successThreshold Min consecutive successes for probe to be successful
## @param livenessProbe.failureThreshold No. consecutive failures before container marked unhealthy
##
livenessProbe:
  enabled: true
  httpGet:
    path: /
    port: http
  initialDelaySeconds: 10
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 10
  successThreshold: 1

## Readiness Probe
## Periodic probe of container service readiness. If the probe fails, container will be removed
## from service endpoints (but will not be restarted unless livenessProbe fails)
##
## @param readinessProbe.enabled Enable readinessProbe for notifications container
## @param readinessProbe.httpGet.path The url path to probe.
## @param readinessProbe.httpGet.port The named containerPort to probe
## @param readinessProbe.periodSeconds Period seconds for readinessProbe
## @param readinessProbe.timeoutSeconds Timeout seconds for readinessProbe
## @param readinessProbe.successThreshold Min consecutive successes for probe to be successful
## @param readinessProbe.failureThreshold No. consecutive failures before container marked unhealthy
##
readinessProbe:
  enabled: true
  httpGet:
    path: /notifications/v1/metrics/ping
    port: http
  initialDelaySeconds: 5
  periodSeconds: 5
  timeoutSeconds: 5
  failureThreshold: 10
  successThreshold: 1

# CURRENTLY IGNORED - NEEDS METRICS SERVICE ENABLED ON CLUSTER
# This section is for setting up autoscaling. More information can be found here:
# https://kubernetes.io/docs/concepts/workloads/autoscaling/
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 10
  # targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

# Additional volumes on the output Deployment definition.
extraVolumes: []

# Additional volumeMounts on the output Deployment definition.
extraVolumeMounts: []

service:
  port: 8080

imagePullSecrets: []
fullnameOverride: ""
nameOverride: ""
nodeSelector: {}
tolerations: []
affinity: {}
resources: {}
# Logging / env map
env: {}
# We usually recommend not to specify default resources and to leave this as a conscious
# choice for the user. This also increases chances charts run on environments with limited
# resources, such as Minikube. If you do want to specify resources, uncomment the following
# lines, adjust them as necessary, and remove the curly braces after 'resources:'.
#resources:
#  limits:
#    cpu: 500m
#    memory: 512Mi
#  requests:
#    cpu: 300m
#    memory: 128Mi

## @section Tomcat Configuration
tomcat:
  ## @param tomcat.extraCatalinaOpts Extra JVM options to pass to Tomcat. SEE IMPORTANT NOTES BELOW:
  ## 1. Do not set JMX-related values here, without first checking they're not already defined in
  ##    tomcat.jmxCatalinaOpts (assuming tomcat.jmxEnabled is true)
  ## 2. You can also set -Xms and/or -Xmx here, but it is safer to set -XX:MaxRAMPercentage instead
  ## 3. Note that -XX:MaxRAMPercentage will be ignored by the JVM if you also set a value for -Xmx
  ##    (i.e. -Xmx takes precedence over MaxRAMPercentage)
  ##
  ## (extraCatalinaOpts is a yaml list, so each entry must be preceded by a dash (-) and a space. It
  ## looks strange when the options also contain dashes, but is correct YAML syntax.)
  ##
  extraCatalinaOpts:
    - -XX:MaxRAMPercentage=75

  ## @param tomcat.jmxEnabled Enable JMX for Tomcat, to inspect JVM usage of CPU, memory, etc.
  ## When JMX is enabled by setting this to 'true', you can view the JMX metrics using a JMX client
  ## such as VisualVM or JConsole, connected via port-forwarding to the port set in tomcat.jmxPort
  ##
  jmxEnabled: false

  ## @param tomcat.jmxPort The port to use for JMX connections. IMPORTANT: If you change this...
  ## 1. you must also change the port in the tomcat.jmxCatalinaOpts section, in the following two
  ##    entries:
  ##     -Dcom.sun.management.jmxremote.port=9010
  ##     -Dcom.sun.management.jmxremote.rmi.port=9010
  ##
  ## 2. If you plan on using a different port # on localhost, it's easiest to use that same port on
  ##    the pod, so you're using a single port for both JMX and RMI.(e.g. instead of port
  ##    forwarding from pod:9010 to localhost:9011, expose jmx on pod:9011 and forward from pod:9011
  ##    to localhost:9011)
  ##
  ## Note that the JMX port is not (and should not be) exposed outside the cluster. It is therefore
  ## necessary to use port-forwarding in order to connect.
  ##
  jmxPort: 9010

  ## @param tomcat.jmxCatalinaOpts [default: see values.yaml] Tomcat JVM options for enabling JMX
  ## Use these to change the port, for example, or to enable authentication.
  ##
  ## IMPORTANT NOTES:
  ## 1. If you change the port numbers here, you must also change the value of tomcat.jmxPort to
  ##    match!
  ## 2. If you plan on using a different port # on localhost, it's easiest to use that same port on
  ##    the pod, so you're using a single port for both JMX and RMI.(e.g. instead of port
  ##    forwarding from pod:9010 to localhost:9011, expose jmx on pod:9011 and forward from pod:9011
  ##    to localhost:9011)
  ##
  ## (jmxCatalinaOpts is a yaml list, so each entry must be preceded by a dash (-) and a space. It
  ## looks strange when the options also contain dashes, but is correct YAML syntax.)
  ##
  jmxCatalinaOpts:
    - -Dcom.sun.management.jmxremote
    - -Dcom.sun.management.jmxremote.port=9010
    - -Dcom.sun.management.jmxremote.rmi.port=9010
    - -Dcom.sun.management.jmxremote.local.only=false
    - -Dcom.sun.management.jmxremote.authenticate=false
    - -Dcom.sun.management.jmxremote.ssl=false
    - -Djava.rmi.server.hostname=127.0.0.1

# Default values for nsconsumer subchart
nsconsumer:
  replicaCount: 1
  nameOverride: "nsconsumer"
  fullNameOverride: "notification-service-consumer"
  image:
    repository: ghcr.io/dataoneorg/nsconsumer
    tag: feature-15
    pullPolicy: IfNotPresent

  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi

  serviceAccount:
    create: false
    name: ""

  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 10
    targetCPUUtilizationPercentage: 50
    targetMemoryUtilizationPercentage: 80

  readinessProbe:
    enabled: true
    exec:
      command:
        - /bin/sh
        - -c
        - test $(($(date +%s) - $(stat -c %Y /tmp/rmq-consumer-readiness))) -lt 30
    initialDelaySeconds: 60
    periodSeconds: 10
    timeoutSeconds: 5
  
  livenessProbe:
    enabled: true
    exec:
      command:
      - /bin/sh
      - -c
      - test $(($(date +%s) - $(stat -c %Y /tmp/rmq-consumer-readiness))) -lt 30
    initialDelaySeconds: 90
    periodSeconds: 10
    timeoutSeconds: 5

## @section RabbitMQ Connection Configuration
## (This section does NOT affect the RabbitMQ Operator installation itself.)
## These settings are used to configure the connection from the nsconsumer app to an existing
## RabbitMQ instance; either the instance deployed by the RabbitMQ Cluster Operator using the YAML
## template in this chart, or an alternative provided nby the user.
##
rmq:
  rmqConnection:
    ## @param rmqConnection.existingSecret Secret holding rmq credentials. Leave blank to autopopulate
    ## from the bundled rabbitmq operator installation (if rmqOperator.enabled)
    ##
    existingSecret: "notification-service-rmq-4-1-3-default-user" # optional: if not using credentials in the default operator-generated secret
    # TODO: autopopulate if not provided

    ## @param rmqConnection.hostname RMQ host to be called by indexer. Leave blank to autopopulate
    ## from the bundled rabbitmq operator installation (if rmqOperator.enabled)
    ##
    hostname: "notification-service-rmq-4-1-3" # TODO: autopopulate if not provided

    ## @param rmqConnection.port RMQ port to be called by consumer
    ##
    port: "5672"

    virtualHost: "/"

    queueName: "dataone.notifications"

    prefetchCount: 25

    ## @param rmqConnection.username The username needed for rabbitmq access. Leave blank to autopopulate
    ## from 'rmqConnection.existingSecret'
    ##
    username: ""  # TODO: autopopulate if not provided

    ## @param rmqConnection.rabbitmqUserKey key associated with rabbitmq username in existing secret
    ##
    rmqUserKey: "username"

    ## @param rmqConnection.rabbitmqPasswordKey key associated with rabbitmq password in existing secret
    ##
    rmqPasswordKey: "password"


  ## @section RabbitMQ Operator Configuration
  ## For a production example of a multi-zone Kubernetes deployment with 3 worker nodes, each in a
  ## different zone, see:
  ## https://github.com/rabbitmq/cluster-operator/tree/main/docs/examples/production-ready
  ##
  rmqOperator:
    enabled: true

    ## @param rmqOperator.nameOverride REQUIRED partial override for resourcename used for pods, PVCs etc
    ## Automatically prepends the release name, so the resulting resource name for the pods etc. will
    ## begin:  <myrelease>-<rabbitmq.nameOverride>-...
    ##
    ## We change this with every change in RabbitMQ app version, so that we can use a new PV/PVC for
    ## each release. This means each new installation is a fresh install, instead of being an upgrade,
    ## which would be a lot more onerous (see rabbitMQ docs).
    ##
    ## Convention is to use 'rmq', followed by the rabbitmq app version, with periods replaced by
    ## dashes; e.g.:
    ##  nameOverride: "rmq-4-1-3"  ## for rabbitmq app version 4.1.3
    ##
    nameOverride: "rmq-4-1-3"

    ## @param rmqOperator.replicaCount Number of desired rabbitmq pods (always use an odd number)
    ## see: https://www.rabbitmq.com/docs/clustering#node-count
    ##
    replicaCount: 1

    ## @param rmqOperator.resources Resource requests and limits for rabbitmq pods (OVERRIDE THESE!)
    ##
    ## NOTE:
    ##   CPU: For production, the ABSOLUTE MINIMUM is 2 CPUs per RabbitMQ node (and 4 CPUs
    ##        RECOMMENDED).
    ##        RMQ will work poorly with 1 CPU, which is why it's not recommended for production
    ##        workloads
    ##   MEMORY: For production, min. 2GB of RAM is recommended, increasing to 4Gi or even 8 - 10Gi,
    ##        depending on load. It is recommended to keep the memory requests and limits both at the
    ##        same value.
    ##
    resources:
      requests:
        cpu: 500m
        memory: 500Mi
      limits:
        cpu: 800m
        memory: 500Mi

    persistence:
      ## @param rmqOperator.persistence.storageClassName Storage class name for rabbitmq data PV
      ## Leave blank ("") to use the default storage class for the cluster
      ##
      storageClassName: ""

      ## @param rmqOperator.persistence Size of persistent volume for rabbitmq data
      ##
      size: 10Gi

    ## @param rmqOperator.envConfig Additional rabbitmq environment variable settings
    ## See:
    ##   https://www.rabbitmq.com/kubernetes/operator/using-operator#env-config
    ##   https://www.rabbitmq.com/docs/configure#customise-environment
    ## Example:
    ##   envConfig:
    ##     RABBITMQ_USE_LONGNAME: "true"
    envConfig: {}

    ## @param rmqOperator.additionalConfig Additional rabbitmq configuration settings
    ## See:
    ##   https://www.rabbitmq.com/docs/configure#config-items
    ##   https://www.rabbitmq.com/docs/production-checklist
    ##   https://github.com/rabbitmq/cluster-operator/tree/main/docs/examples/production-ready
    ##
    additionalConfig:
      disk_free_limit.relative: "1.0"


  nodeSelector: {}

  tolerations: []

  affinity: {}
